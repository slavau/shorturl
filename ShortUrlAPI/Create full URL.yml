info:
  name: Create full URL
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:8080/url/full?shortUrl
  params:
    - name: shortUrl
      value: ""
      type: query
  body:
    type: json
    data: |-
      {
        "url": "https://www.example.com/very/long/url"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
