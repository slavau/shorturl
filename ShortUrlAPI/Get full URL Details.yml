info:
  name: Get full URL Details
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:8080/api/v1/url/full?shortUrl=http://localhost:8080/DGxEOIg
  params:
    - name: shortUrl
      value: http://localhost:8080/DGxEOIg
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
